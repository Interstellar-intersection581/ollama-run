{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path'); echo \"$f\" | grep -q '\\.py$' && python3 -m py_compile \"$f\" 2>&1 | head -5 || true",
            "statusMessage": "Checking Python syntax..."
          }
        ]
      }
    ]
  }
}
